Act as a financial data extraction expert. Your task is to extract ONLY executed stock and ETF trades from the provided brokerage statement and output them as a JSON array. Next to the pdf document you have also text layer extracted locally. If you find conflicts between the text recognized from the PDF and the locally extracted text, ALWAYS prioritize the EXACT values from the text layer as it is more accurate.

CRITICAL EXTRACTION RULES:

1. STRICT DEFINITION OF A TRADE: You must ONLY extract explicit buy or sell orders (they are usually in one table). To be considered a valid trade, the line item MUST contain all of the following: 
- A specific execution date
- A security identifier (ISIN, ticker, or security name) and that MUST be an exchange traded fund, commodity or stock
- A transaction action keyword (e.g., "Buy", "Sell", "Bought", "Sold" or similar, in any language, but it must be clear that it is a trade action)
- A traded quantity
- An execution price (or total monetary amount).

2. WHAT TO IGNORE (OMIT ENTIRELY): Completely ignore "Portfolio Holdings", "Positions", "Cash Balances", "Dividends", "Interest", "Miscellaneous Activity", and account fees. 
- STRICT FOREX BAN: Completely ignore Forex transactions, currency conversions, and currency pairs (e.g. USD.EUR). 
- DO NOT create a JSON object for ignored items. NEVER create an entry just to write "Ignored" or "Forex transaction" in the 'extra_info' field. If a transaction is invalid, it must NOT exist in the final JSON array.

3. EMPTY STATE: If there are no valid buy/sell trades in the document (e.g., the document only contains holdings and dividends), you must return an empty JSON array exactly like this: []

4. GROUPED TABLES: Sometimes the list of trades is composed of multiple tables containing rows related to a given security and its subtotals. Don't focus only on a first such a section but of course ignore the subtotals when extracting ALL of the trades.


JSON SCHEMA REQUIREMENT:

For every valid trade identified, return an object within a JSON output (see scheme provided) containing the following fields:

- 'broker': The name of the brokerage (if identifiable from the document, otherwise null) - doesn't repeat for all the trades in the same document, but can be added as a top-level field in the JSON.
- 'position': The position of transaction in the document, starting with 1 for the first transaction. This is critical for traceability and auditing purposes. Never aggregate multiple transactions into one entry, even if they involve the same ticker or ISIN, as they are distinct for tax purposes.
- 'date': Output as YYYY-MM-DD. If you find different formats, identify and convert them accurately. If you find multiple dates for the given transaction, pick the earliest avialable (as the later one might be dates related to settlement etc.). Do not approximate.
- 'isin': The 12-character International Securities Identification Number (ISIN). If not present in the document, keep it as null.
- 'ticker': The ticker symbol MUST be 1-5 capital characters, can include dot or numbers, often in Instrument or Symbol column (if not available, keep it as null). Also ALWAYS specify in 'extra_info' if this value was present or e.g. derived from the security name or ISIN.
- 'security_name': The name or description of the stock/ETF, for stocks it can be similar to ticker but is typically much longer text. Always keep it as null in case only value that is compatible with ISIN is found or the value would be exactly the same as for 'isin' value.
- 'quantity': In case it is a buy operation use positive number for quantity, if it is sell operation use negative number. If the transaction involving the same ticker, ISIN or security_name, DO NOT aggregate them into a single entry, keep them separate as they are distinct transactions for tax purposes.
- 'price': Extract the exact execution or trade price per unit/share. If not present, keep it as null.
- 'fee': Extract the fees or commissions for the particular trade if they are specified directly. 
- 'value': Do not calculate this. You MUST extract the printed gross or net total value of the transaction directly from the document row. Do not perform any multiplication. If the row contains no total monetary value, keep it as null.
- 'currency': The 3-letter ISO-4217 currency code (e.g., EUR, USD, GBP). If not present directly, try to infer it from the format of the value ($ = USD, € = EUR, £ = GBP etc.). NEVER derive currency based on the typical currency of the security with a given ticker and/or isin unless you can use a local context (formatting of the values or if the trade is listed for example in the EUR subsection the currency is logically EUR). If completely unidentifiable, keep it as null.
- 'tax_rate': Expected tax rate for TOB in Belgium based on following rules (depends on Country of registration and Distributing or Accumulating of dividends):
    Stocks - tax_rate=0.0035
    Belgium - Distributing - tax_rate=0.0012
    Belgium - Accumulating - tax_rate=0.0132
    European Economic Area (EEA) excluding Belgium - Distributing or Accumulating - tax_rate=0.0012
    Outside EEA - Distributing or Accumulating - tax_rate=0.0035
    But:
    - ETFs that invest in commodities or currencies, also called ETCs, have a tax_rate=0.0035.
    - Exchange-traded notes (ETN) have a tax_rate=0.0012.
- 'extra_info': Any additional information or notes about the transaction relevant for security identification.
- 'original_text': The original text snippet from the text layer (not the PDF that might contain visual errors) that corresponds to this transaction. This is critical for traceability and auditing, allowing users to verify the source of each extracted trade. Do not modify or clean this text, as it is important for users to see the raw extracted data for each transaction.